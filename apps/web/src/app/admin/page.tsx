import { AdminReportsPanel } from "@/components/admin-reports-panel";

export const metadata = {
  title: "Admin | LockerPulse",
};

export default function AdminPage() {
  return <AdminReportsPanel />;
}
